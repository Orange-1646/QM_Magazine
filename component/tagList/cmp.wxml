<view class="container" catch:tap="log">
  <slot name="before" />
  <c-tag class="tag" wx:for="{{tagArr}}" tag="{{item.type||item.tag}}" tagId="{{item.typeId||item.tagId}}" wx:key="{{index}}" />
  <slot name="after" />
</view>
